services:
  - type: web
    name: ai-ids-backend
    runtime: python
    rootDir: web/api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn index:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.9.0"
