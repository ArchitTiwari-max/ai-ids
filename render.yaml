services:
  - type: web
    name: ai-ids-backend
    runtime: python
    buildCommand: pip install -r web/api/requirements.txt
    startCommand: cd web/api && uvicorn index:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.9.0"
